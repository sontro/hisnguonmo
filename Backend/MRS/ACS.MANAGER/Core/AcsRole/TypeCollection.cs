using ACS.EFMODEL.DataModels;
using System;
using System.Collections.Generic;

namespace ACS.MANAGER.Core.AcsRole
{
    partial class TypeCollection
    {
        internal static readonly List<Type> AcsRole = new List<Type>() { typeof(ACS_ROLE), typeof(List<ACS_ROLE>), typeof(List<ACS_ROLE_BASE>) };
    }
}
