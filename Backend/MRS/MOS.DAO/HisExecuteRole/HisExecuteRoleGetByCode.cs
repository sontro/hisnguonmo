using MOS.DAO.Base;
using MOS.DAO.StagingObject;
using MOS.EFMODEL.DataModels;
using Inventec.Common.Logging;
using Inventec.Core;
using System;
using System.Collections.Generic;
using System.Linq;

namespace MOS.DAO.HisExecuteRole
{
    partial class HisExecuteRoleGet : EntityBase
    {
        public HIS_EXECUTE_ROLE GetByCode(string code, HisExecuteRoleSO search)
        {
            HIS_EXECUTE_ROLE result = null;
            try
            {
                bool valid = true;
                valid = valid && IsNotNullOrEmpty(code);
                if (valid)
                {
                    using (var ctx = new MOS.DAO.Base.AppContext())
                    {
                        var query = ctx.HIS_EXECUTE_ROLE.AsQueryable().Where(p => p.EXECUTE_ROLE_CODE == code);
                        if (search.listHisExecuteRoleExpression != null && search.listHisExecuteRoleExpression.Count > 0)
                        {
                            foreach (var item in search.listHisExecuteRoleExpression)
                            {
                                query = query.Where(item);
                            }
                        }
                        result = query.SingleOrDefault();
                    }
                }
            }
            catch (Exception ex)
            {
                Logging(Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => code), code) + Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => search), search), LogType.Error);
                LogSystem.Error(ex);
                result = null;
            }
            return result;
        }
    }
}
