using SDA.DAO.Base;
using SDA.DAO.StagingObject;
using SDA.EFMODEL.DataModels;
using Inventec.Common.Logging;
using Inventec.Core;
using System;
using System.Collections.Generic;
using System.Linq;

namespace SDA.DAO.SdaGroupType
{
    partial class SdaGroupTypeGet : EntityBase
    {
        public Dictionary<string, SDA_GROUP_TYPE> GetDicByCode(SdaGroupTypeSO search, CommonParam param)
        {
            Dictionary<string, SDA_GROUP_TYPE> dic = new Dictionary<string, SDA_GROUP_TYPE>();
            try
            {
                List<SDA_GROUP_TYPE> listRecord = Get(search, param);
                if (listRecord != null)
                {
                    foreach (var item in listRecord)
                    {
                        if (!dic.ContainsKey(item.GROUP_TYPE_CODE))
                        {
                            dic.Add(item.GROUP_TYPE_CODE, item);
                        }
                    }
                }
            }
            catch (Exception ex)
            {
                Logging(Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => search), search) + Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => param), param), LogType.Error);
                LogSystem.Error(ex);
                dic.Clear();
            }
            return dic;
        }
    }
}
