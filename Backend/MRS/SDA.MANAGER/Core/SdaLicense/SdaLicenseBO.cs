using System;

namespace SDA.MANAGER.Core.SdaLicense
{
    partial class SdaLicenseBO : BusinessObjectBase
    {
        internal SdaLicenseBO()
            : base()
        {

        }

        internal T Get<T>(object data)
        {
            T result = default(T);
            try
            {
                IDelegacyT delegacy = new SdaLicenseGet(param, data);
                result = delegacy.Execute<T>();
            }
            catch (Exception ex)
            {
                Inventec.Common.Logging.LogSystem.Error(ex);
                result = default(T);
            }
            return result;
        }
    }
}
