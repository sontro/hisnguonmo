using System;

namespace SDA.MANAGER.Core.SdaConfig
{
    partial class SdaConfigBO : BusinessObjectBase
    {
        internal SdaConfigBO()
            : base()
        {

        }

        internal T Get<T>(object data)
        {
            T result = default(T);
            try
            {
                IDelegacyT delegacy = new SdaConfigGet(param, data);
                result = delegacy.Execute<T>();
            }
            catch (Exception ex)
            {
                Inventec.Common.Logging.LogSystem.Error(ex);
                result = default(T);
            }
            return result;
        }
    }
}
