using System;

namespace SDA.MANAGER.Core.SdaGroup
{
    partial class SdaGroupBO : BusinessObjectBase
    {
        internal SdaGroupBO()
            : base()
        {

        }

        internal T Get<T>(object data)
        {
            T result = default(T);
            try
            {
                IDelegacyT delegacy = new SdaGroupGet(param, data);
                result = delegacy.Execute<T>();
            }
            catch (Exception ex)
            {
                Inventec.Common.Logging.LogSystem.Error(ex);
                result = default(T);
            }
            return result;
        }
    }
}
