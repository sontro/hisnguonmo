using System;

namespace SDA.MANAGER.Core.SdaGroupType
{
    partial class SdaGroupTypeBO : BusinessObjectBase
    {
        internal SdaGroupTypeBO()
            : base()
        {

        }

        internal T Get<T>(object data)
        {
            T result = default(T);
            try
            {
                IDelegacyT delegacy = new SdaGroupTypeGet(param, data);
                result = delegacy.Execute<T>();
            }
            catch (Exception ex)
            {
                Inventec.Common.Logging.LogSystem.Error(ex);
                result = default(T);
            }
            return result;
        }
    }
}
