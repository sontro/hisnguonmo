using MOS.DAO.Base;
using MOS.DAO.StagingObject;
using MOS.EFMODEL.DataModels;
using Inventec.Common.Logging;
using Inventec.Core;
using System;
using System.Collections.Generic;
using System.Linq;

namespace MOS.DAO.HisConfigGroup
{
    partial class HisConfigGroupGet : EntityBase
    {
        public HIS_CONFIG_GROUP GetByCode(string code, HisConfigGroupSO search)
        {
            HIS_CONFIG_GROUP result = null;
            try
            {
                bool valid = true;
                valid = valid && IsNotNullOrEmpty(code);
                if (valid)
                {
                    using (var ctx = new AppContext())
                    {
                        var query = ctx.HIS_CONFIG_GROUP.AsQueryable().Where(p => p.CONFIG_GROUP_CODE == code);
                        if (search.listHisConfigGroupExpression != null && search.listHisConfigGroupExpression.Count > 0)
                        {
                            foreach (var item in search.listHisConfigGroupExpression)
                            {
                                query = query.Where(item);
                            }
                        }
                        result = query.SingleOrDefault();
                    }
                }
            }
            catch (Exception ex)
            {
                Logging(Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => code), code) + Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => search), search), LogType.Error);
                LogSystem.Error(ex);
                result = null;
            }
            return result;
        }
    }
}
