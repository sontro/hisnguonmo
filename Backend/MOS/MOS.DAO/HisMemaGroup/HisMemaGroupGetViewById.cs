using MOS.DAO.Base;
using MOS.DAO.StagingObject;
using MOS.EFMODEL.DataModels;
using Inventec.Common.Logging;
using Inventec.Core;
using System;
using System.Collections.Generic;
using System.Linq;

namespace MOS.DAO.HisMemaGroup
{
    partial class HisMemaGroupGet : EntityBase
    {
        public V_HIS_MEMA_GROUP GetViewById(long id, HisMemaGroupSO search)
        {
            V_HIS_MEMA_GROUP result = null;
            try
            {
                bool valid = true;
                valid = valid && IsGreaterThanZero(id);
                if (valid)
                {
                    using (var ctx = new AppContext())
                    {
                        var query = ctx.V_HIS_MEMA_GROUP.AsQueryable().Where(p => p.ID == id);
                        if (search.listVHisMemaGroupExpression != null && search.listVHisMemaGroupExpression.Count > 0)
                        {
                            foreach (var item in search.listVHisMemaGroupExpression)
                            {
                                query = query.Where(item);
                            }
                        }
                        result = query.SingleOrDefault();
                    }
                }
            }
            catch (Exception ex)
            {
                Logging(Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => id), id) + Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => search), search), LogType.Error);
                LogSystem.Error(ex);
                result = null;
            }
            return result;
        }
    }
}
