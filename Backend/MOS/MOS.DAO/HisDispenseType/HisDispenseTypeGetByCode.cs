using MOS.DAO.Base;
using MOS.DAO.StagingObject;
using MOS.EFMODEL.DataModels;
using Inventec.Common.Logging;
using Inventec.Core;
using System;
using System.Collections.Generic;
using System.Linq;

namespace MOS.DAO.HisDispenseType
{
    partial class HisDispenseTypeGet : EntityBase
    {
        public HIS_DISPENSE_TYPE GetByCode(string code, HisDispenseTypeSO search)
        {
            HIS_DISPENSE_TYPE result = null;
            try
            {
                bool valid = true;
                valid = valid && IsNotNullOrEmpty(code);
                if (valid)
                {
                    using (var ctx = new AppContext())
                    {
                        var query = ctx.HIS_DISPENSE_TYPE.AsQueryable().Where(p => p.DISPENSE_TYPE_CODE == code);
                        if (search.listHisDispenseTypeExpression != null && search.listHisDispenseTypeExpression.Count > 0)
                        {
                            foreach (var item in search.listHisDispenseTypeExpression)
                            {
                                query = query.Where(item);
                            }
                        }
                        result = query.SingleOrDefault();
                    }
                }
            }
            catch (Exception ex)
            {
                Logging(Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => code), code) + Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => search), search), LogType.Error);
                LogSystem.Error(ex);
                result = null;
            }
            return result;
        }
    }
}
