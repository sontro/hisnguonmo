using SDA.DAO.Base;
using SDA.DAO.StagingObject;
using SDA.EFMODEL.DataModels;
using Inventec.Common.Logging;
using Inventec.Core;
using System;
using System.Collections.Generic;
using System.Linq;

namespace SDA.DAO.SdaDeleteData
{
    partial class SdaDeleteDataGet : EntityBase
    {
        public Dictionary<string, SDA_DELETE_DATA> GetDicByCode(SdaDeleteDataSO search, CommonParam param)
        {
            Dictionary<string, SDA_DELETE_DATA> dic = new Dictionary<string, SDA_DELETE_DATA>();
            try
            {
                List<SDA_DELETE_DATA> listRecord = Get(search, param);
                if (listRecord != null)
                {
                    foreach (var item in listRecord)
                    {
                        if (!dic.ContainsKey(item.DELETE_DATA_CODE))
                        {
                            dic.Add(item.DELETE_DATA_CODE, item);
                        }
                    }
                }
            }
            catch (Exception ex)
            {
                Logging(Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => search), search) + Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => param), param), LogType.Error);
                LogSystem.Error(ex);
                dic.Clear();
            }
            return dic;
        }
    }
}
