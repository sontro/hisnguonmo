using SDA.DAO.Base;
using SDA.DAO.StagingObject;
using SDA.EFMODEL.DataModels;
using Inventec.Common.Logging;
using Inventec.Core;
using System;
using System.Collections.Generic;
using System.Linq;

namespace SDA.DAO.SdaGroupType
{
    partial class SdaGroupTypeGet : EntityBase
    {
        public V_SDA_GROUP_TYPE GetViewByCode(string code, SdaGroupTypeSO search)
        {
            V_SDA_GROUP_TYPE result = null;
            try
            {
                bool valid = true;
                valid = valid && IsNotNullOrEmpty(code);
                if (valid)
                {
                    using (var ctx = new AppContext())
                    {
                        var query = ctx.V_SDA_GROUP_TYPE.AsQueryable().Where(p => p.GROUP_TYPE_CODE == code);
                        if (search.listVSdaGroupTypeExpression != null && search.listVSdaGroupTypeExpression.Count > 0)
                        {
                            foreach (var item in search.listVSdaGroupTypeExpression)
                            {
                                query = query.Where(item);
                            }
                        }
                        result = query.SingleOrDefault();
                    }
                }
            }
            catch (Exception ex)
            {
                Logging(Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => code), code) + Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => search), search), LogType.Error);
                LogSystem.Error(ex);
                result = null;
            }
            return result;
        }
    }
}
