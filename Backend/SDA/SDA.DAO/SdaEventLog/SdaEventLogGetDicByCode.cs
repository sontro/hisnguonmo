using SDA.DAO.Base;
using SDA.DAO.StagingObject;
using SDA.EFMODEL.DataModels;
using Inventec.Common.Logging;
using Inventec.Core;
using System;
using System.Collections.Generic;
using System.Linq;

namespace SDA.DAO.SdaEventLog
{
    partial class SdaEventLogGet : EntityBase
    {
        public Dictionary<string, SDA_EVENT_LOG> GetDicByCode(SdaEventLogSO search, CommonParam param)
        {
            Dictionary<string, SDA_EVENT_LOG> dic = new Dictionary<string, SDA_EVENT_LOG>();
            try
            {
                List<SDA_EVENT_LOG> listRecord = Get(search, param);
                if (listRecord != null)
                {
                    foreach (var item in listRecord)
                    {
                        if (!dic.ContainsKey(item.EVENT_LOG_CODE))
                        {
                            dic.Add(item.EVENT_LOG_CODE, item);
                        }
                    }
                }
            }
            catch (Exception ex)
            {
                Logging(Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => search), search) + Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => param), param), LogType.Error);
                LogSystem.Error(ex);
                dic.Clear();
            }
            return dic;
        }
    }
}
