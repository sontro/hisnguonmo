using ACS.EFMODEL.DataModels;
using System;
using System.Collections.Generic;

namespace ACS.MANAGER.Core.AcsControl
{
    partial class TypeCollection
    {
        internal static readonly List<Type> AcsControl = new List<Type>() { typeof(ACS_CONTROL), typeof(List<ACS_CONTROL>) };
    }
}
