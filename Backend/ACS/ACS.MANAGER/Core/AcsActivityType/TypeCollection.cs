using ACS.EFMODEL.DataModels;
using System;
using System.Collections.Generic;

namespace ACS.MANAGER.Core.AcsActivityType
{
    partial class TypeCollection
    {
        internal static readonly List<Type> AcsActivityType = new List<Type>() { typeof(ACS_ACTIVITY_TYPE), typeof(List<ACS_ACTIVITY_TYPE>) };
    }
}
