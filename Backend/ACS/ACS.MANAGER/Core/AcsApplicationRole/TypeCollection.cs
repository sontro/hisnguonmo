using ACS.EFMODEL.DataModels;
using System;
using System.Collections.Generic;

namespace ACS.MANAGER.Core.AcsApplicationRole
{
    partial class TypeCollection
    {
        internal static readonly List<Type> AcsApplicationRole = new List<Type>() { typeof(ACS_APPLICATION_ROLE), typeof(List<ACS_APPLICATION_ROLE>), typeof(List<long>) };
    }
}
