using ACS.EFMODEL.DataModels;
using System;
using System.Collections.Generic;

namespace ACS.MANAGER.Core.AcsActivityLog
{
    partial class TypeCollection
    {
        internal static readonly List<Type> AcsActivityLog = new List<Type>() { typeof(ACS_ACTIVITY_LOG), typeof(List<ACS_ACTIVITY_LOG>) };
    }
}
