using ACS.EFMODEL.DataModels;
using System;
using System.Collections.Generic;

namespace ACS.MANAGER.Core.AcsCredentialData
{
    partial class TypeCollection
    {
        internal static readonly List<Type> AcsCredentialData = new List<Type>() { typeof(ACS_CREDENTIAL_DATA), typeof(List<ACS_CREDENTIAL_DATA>) };
    }
}
