using ACS.EFMODEL.DataModels;
using System;
using System.Collections.Generic;

namespace ACS.MANAGER.Core.AcsControlRole
{
    partial class TypeCollection
    {
        internal static readonly List<Type> AcsControlRole = new List<Type>() { typeof(ACS_CONTROL_ROLE), typeof(List<ACS_CONTROL_ROLE>), typeof(List<long>) };
    }
}
