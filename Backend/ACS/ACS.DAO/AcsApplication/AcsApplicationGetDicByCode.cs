using ACS.DAO.Base;
using ACS.DAO.StagingObject;
using ACS.EFMODEL.DataModels;
using Inventec.Common.Logging;
using Inventec.Core;
using System;
using System.Collections.Generic;
using System.Linq;

namespace ACS.DAO.AcsApplication
{
    partial class AcsApplicationGet : EntityBase
    {
        public Dictionary<string, ACS_APPLICATION> GetDicByCode(AcsApplicationSO search, CommonParam param)
        {
            Dictionary<string, ACS_APPLICATION> dic = new Dictionary<string, ACS_APPLICATION>();
            try
            {
                List<ACS_APPLICATION> listRecord = Get(search, param);
                if (listRecord != null)
                {
                    foreach (var item in listRecord)
                    {
                        if (!dic.ContainsKey(item.APPLICATION_CODE))
                        {
                            dic.Add(item.APPLICATION_CODE, item);
                        }
                    }
                }
            }
            catch (Exception ex)
            {
                Logging(Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => search), search) + Inventec.Common.Logging.LogUtil.TraceData(Inventec.Common.Logging.LogUtil.GetMemberName(() => param), param), LogType.Error);
                LogSystem.Error(ex);
                dic.Clear();
            }
            return dic;
        }
    }
}
